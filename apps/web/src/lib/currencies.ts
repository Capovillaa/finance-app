/**
 * A short list rather than the full ISO 4217 set — nobody should have to
 * scroll 180 codes to pick a currency. Used at registration and when creating
 * accounts; workspace base currency is editable later in settings.
 */
export const COMMON_CURRENCIES = ['USD', 'EUR', 'GBP', 'BRL', 'CAD', 'AUD', 'JPY'];
